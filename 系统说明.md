# 私域运营 Agent 中台 · 系统说明

面向代运营公司的私域运营中台：多行业适配、指令驱动、AI 生成策略资产包、整月排期、到点自动下发、人工验收闭环。

## 整体架构

```
┌──────────────────────────────────────────────────────────┐
│                      前端 Web (React)                    │
│   总览·会话·需求飞轮·指令中心·执行中心·知识库             │
│   策略沉淀·LLM网关·客户画像·护栏·配置·开发者后台          │
└──────────────────────┬───────────────────────────────────┘
                       │ REST API
┌──────────────────────┴───────────────────────────────────┐
│                  后端服务 (FastAPI)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐  │
│  │ 指令引擎 │ │ 执行调度 │ │ 护栏引擎 │ │ 策略自生长  │  │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐  │
│  │ 知识检索 │ │ 客户画像 │ │ 渠道适配 │ │ 飞书/企微桥 │  │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘  │
└──────┬─────────────┬──────────────┬─────────────┬────────┘
       │             │              │             │
   ┌───┴───┐   ┌──────┴──────┐ ┌────┴────┐  ┌─────┴─────┐
   │ LLM   │   │ 向量数据库  │ │ 业务数据库│ │ 渠道 API  │
   │ 网关  │   │ (Embedding) │ │ SQLite/PG│ │ 飞书/企微 │
   └───────┘   └─────────────┘ └──────────┘  └───────────┘
```

LLM 网关兼容 OpenAI 协议，支持 Ollama / DeepSeek / 混元 / GPT 切换，向量检索支持本地或云端模型。

## 核心模块

| 模块 | 作用 |
|------|------|
| 指令中心 | 12 维结构化指令输入，调用 LLM 生成 7 板块策略资产包 |
| 执行中心 | 按活动/货盘/1v1/朋友圈/社群/短信/跟进分类展示，整月排期自动调度 |
| 需求飞轮 | 热点信号采集 + 行业洞察，为人工下达指令提供参考依据 |
| 策略沉淀中心 | 可复用策略库，支持策略自生长与跨行业复用 |
| 知识库 | 多行业知识 + 品牌资料，资产包生成时自动检索真实素材 |
| 护栏 | 品牌违禁词 + 语境放行，命中自动拦截告警 |
| 客户画像 | 客户身份/需求/消费标签结构化沉淀 |
| 行业配置 | 美业/餐饮/零售/教育/宠物/大健康模板，切换即全系统适配 |
| 开发者后台 | 神经元中枢：平台共享策略管理、多租户隔离、模型配置 |
| LLM 网关 | 多模型路由配置，按成本与质量灵活切换 |

## 界面导航

```
私域运营 Agent 中台
├── 总览          系统数据看板
├── 会话          对话式指令输入
├── 需求飞轮       市场信号 → 行业洞察 → 策略建议
├── 指令中心       飞轮洞察建议 / 新指令 / 指令列表
├── 执行中心       活动·货盘·1v1·朋友圈·社群·短信·跟进
├── 知识库        文档/卡项/话术/护理项目
├── 策略沉淀中心    可复用策略管理
├── LLM 网关      模型路由与成本配置
├── 客户画像       客户标签与消费记录
├── 护栏          违禁词与放行规则
├── 配置          系统设置
└── 开发者后台      平台级策略/租户/神经元管理
```

## 运行流程

1. 需求飞轮采集市场信号，产出洞察建议
2. 运营人员参考洞察，在指令中心下达指令（可上传品牌素材）
3. 系统检索知识库 + 调用 LLM，生成 7 板块策略资产包
4. 运营审阅资产包，批准后进入执行中心
5. 执行中心生成整月排期待办，后台到点自动下发
6. 内容经护栏校验，下发至飞书/企微等渠道
7. 验收阶段回填 KPI 实际值，生成达成率报告，回传策略引擎自迭代

## 技术栈

后端：Python 3.12 · FastAPI · SQLAlchemy 2 · Alembic · SQLite/PostgreSQL
前端：React · Vite · TypeScript
模型：OpenAI 兼容协议 · Ollama / DeepSeek / 混元 / GPT
部署：Docker · macOS LaunchAgent · 云端可迁移

## 多租户架构

采用「神经元中枢 + 品牌分支」模型：开发者后台为神经元本体，平台级策略/模板默认共享；品牌为分支，业务数据完全隔离，可覆盖私有内容。支持 AI 变异生成新策略，人工审核后入库自生长。

## 登录信息

管理员：admin / admin123（首次登录后请修改密码）
演示账号：demo / demo123（只读，仅展示）
生产地址由后端托管，默认 http://localhost:8000

完整开发方案见 docs/开发方案.md，迭代规划见 中台迭代2.0方案.html。
